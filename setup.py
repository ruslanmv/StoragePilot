#!/usr/bin/env python3
"""
Setup script for StoragePilot (backward compatibility with older pip).

This project uses pyproject.toml as the primary build configuration.
This setup.py is provided for compatibility with older tools.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
